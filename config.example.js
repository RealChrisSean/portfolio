/**
 * YouTube API Configuration
 *
 * SETUP INSTRUCTIONS:
 * 1. Copy this file and rename it to config.js
 * 2. Replace 'your_youtube_api_key_here' with your actual YouTube API key
 * 3. NEVER commit config.js to git (it's already in .gitignore)
 *
 * Get your YouTube API key:
 * https://console.cloud.google.com/apis/credentials
 *
 * SECURITY NOTE:
 * The placeholder-checker.js script will automatically detect placeholder data
 * in content.js and fetch real data from YouTube API when config.js is present.
 * This only works locally - on GitHub Pages, all data must be pre-filled in content.js
 * to avoid exposing your API key.
 */

const CONFIG = {
    YOUTUBE_API_KEY: 'your_youtube_api_key_here'
};
